<UserControl x:Class="App2.Views.SalesInvoiceDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:App2.Views"
             mc:Ignorable="d"
             Background="#F8FAFC"
             FlowDirection="RightToLeft">

    <UserControl.Resources>

        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#0F172A" Opacity="0.03" BlurRadius="6" ShadowDepth="1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F1F5F9"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="RowHeight" Value="45"/>
            <Setter Property="ColumnHeaderHeight" Value="45"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                    <Setter Property="Foreground" Value="#1E40AF"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Grid Margin="24">
        <Border Style="{StaticResource ModernCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Padding="16" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Background="#D1FAE5" CornerRadius="8" Padding="8" Margin="0,0,12,0">
                                <TextBlock Text="📄" FontSize="18" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="تفاصيل فاتورة المبيعات" FontSize="18" FontWeight="Bold" Foreground="#0F172A" VerticalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                            <StackPanel Margin="0,0,32,0">
                                <TextBlock Text="العميل" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Right"/>
                                <TextBlock Text="{Binding Invoice.Customer.Name}" FontSize="14" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,2,0,0" HorizontalAlignment="Right"/>
                            </StackPanel>

                            <StackPanel Margin="0,0,32,0">
                                <TextBlock Text="التاريخ" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Right"/>
                                <TextBlock Text="{Binding Invoice.InvoiceDate, StringFormat=yyyy/MM/dd}" FontSize="14" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,2,0,0" HorizontalAlignment="Right"/>
                            </StackPanel>

                            <StackPanel>
                                <TextBlock Text="رقم الفاتورة" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Right"/>
                                <TextBlock Text="{Binding Invoice.InvoiceNumber}" FontSize="14" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,2,0,0" HorizontalAlignment="Right"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Padding="16,12" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <TextBlock Text="الإجمالي" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Invoice.Total, StringFormat=N2}" FontSize="15" FontWeight="Bold" Foreground="#1E293B" Margin="0,2,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                            <TextBlock Text="الخصم" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Invoice.Discount, StringFormat=N2}" FontSize="15" FontWeight="SemiBold" Foreground="#EF4444" Margin="0,2,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                            <TextBlock Text="مدفوع نقداً" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Invoice.PaidInCash, StringFormat=N2}" FontSize="15" FontWeight="SemiBold" Foreground="#10B981" Margin="0,2,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                            <TextBlock Text="مدفوع تحويل" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Invoice.Transfer, StringFormat=N2}" FontSize="15" FontWeight="SemiBold" Foreground="#3B82F6" Margin="0,2,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="4" HorizontalAlignment="Center">
                            <TextBlock Text="المتبقي (الآجل)" FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Invoice.Deferred, StringFormat=N2}" FontSize="15" FontWeight="Black" Foreground="#F59E0B" Margin="0,2,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="الأصناف المباعة" FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" Margin="20,16,20,8"/>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" Margin="20,0,20,20">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="رقم الصنف / الخيط" Binding="{Binding ThreadNumber}" Width="2*"/>
                            <DataGridTextColumn Header="الوحدة" Binding="{Binding UnitName}" Width="*"/>
                            <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity, StringFormat=N2}" Width="*"/>
                            <DataGridTextColumn Header="سعر الوحدة" Binding="{Binding Price, StringFormat=N2}" Width="*"/>
                            <DataGridTextColumn Header="الإجمالي" Binding="{Binding TotalPrice, StringFormat=N2}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

            </Grid>
        </Border>
    </Grid>
</UserControl>