<Window x:Class="App2.Views.LicenseActivationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تفعيل الترخيص" Height="700" Width="550"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F8FAFC" FontFamily="Segoe UI" FlowDirection="RightToLeft">

    <Window.Resources>
        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#94A3B8"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#10B981"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Padding="40" Background="White" Margin="20" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1">
        <StackPanel>
            <StackPanel HorizontalAlignment="Center" Margin="0,0,0,30">
                <Border Background="#ECFDF5" CornerRadius="40" Width="80" Height="80" Margin="0,0,0,15">
                    <TextBlock Text="🔐" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#10B981"/>
                </Border>
                <TextBlock Text="تفعيل النظام" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>
            </StackPanel>

            <TextBlock Text="معرف الجهاز (Hardware ID)" Foreground="#475569" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <Grid Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="HardwareIdTextBox" Style="{StaticResource RoundedTextBoxStyle}" IsReadOnly="True" Background="#F1F5F9" FontFamily="Consolas" Height="45"/>
                <Button Grid.Column="1" Margin="8,0,0,0" Content="📋" Click="CopyHardwareIdButton_Click" Background="#64748B" Style="{StaticResource PrimaryButtonStyle}"/>
            </Grid>

            <TextBlock Text="مفتاح الترخيص" Foreground="#475569" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBox x:Name="LicenseKeyTextBox" Style="{StaticResource RoundedTextBoxStyle}" Height="150" TextWrapping="Wrap" AcceptsReturn="True" FontFamily="Consolas" Margin="0,0,0,5"/>
            <TextBlock Text="قم بلصق مفتاح الترخيص المكون من 32 رمزاً" FontSize="12" Foreground="#94A3B8" Margin="0,0,0,30"/>

            <Button x:Name="ActivateButton" Content="تفعيل الآن" Click="ActivateButton_Click" Height="55" Style="{StaticResource PrimaryButtonStyle}"/>

            <TextBlock Text="تحتاج مساعدة؟ تواصل مع الدعم الفني" FontSize="13" Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,25,0,0" Cursor="Hand"/>
        </StackPanel>
    </Border>
</Window>