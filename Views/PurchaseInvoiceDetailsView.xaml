<UserControl x:Class="App2.Views.PurchaseInvoiceDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:App2.Views"
        mc:Ignorable="d"
        Background="#F8FAFC"
        FlowDirection="RightToLeft">

    <UserControl.Resources>
        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#0F172A" Opacity="0.03" BlurRadius="6" ShadowDepth="1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F1F5F9"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="RowHeight" Value="45"/>
            <Setter Property="ColumnHeaderHeight" Value="45"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>
        
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
        </Style>
        
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                    <Setter Property="Foreground" Value="#1E40AF"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource ModernCard}" Margin="0,0,0,24" Padding="24">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <Border Background="#EEF2FF" CornerRadius="8" Padding="12" Margin="0,0,16,0">
                        <TextBlock Text="👁️" FontSize="24" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="تفاصيل فاتورة الشراء" FontSize="24" FontWeight="Bold" Foreground="#0F172A"/>
                        <TextBlock Text="{Binding Invoice.InvoiceNumber, StringFormat='رقم الفاتورة: \{0\}'}" FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Invoice Info -->
        <Border Grid.Row="1" Style="{StaticResource ModernCard}" Margin="0,0,0,24" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="رقم الفاتورة" FontSize="12" Foreground="#94A3B8"/>
                    <TextBlock Text="{Binding Invoice.InvoiceNumber}" FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,5,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1">
                    <TextBlock Text="التاريخ" FontSize="12" Foreground="#94A3B8"/>
                    <TextBlock Text="{Binding Invoice.InvoiceDate, StringFormat=yyyy/MM/dd}" FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="رقم الحاوية" FontSize="12" Foreground="#94A3B8"/>
                    <TextBlock Text="{Binding Invoice.ContainerNumber}" FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="3">
                    <TextBlock Text="التصنيف" FontSize="12" Foreground="#94A3B8"/>
                    <TextBlock Text="{Binding Invoice.Category}" FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Items DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource ModernCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="الأصناف" FontSize="18" FontWeight="SemiBold" Foreground="#1E293B" Margin="20,20,20,10"/>
                
                <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" Margin="20,0,20,20">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="رقم الكرتون" Binding="{Binding BoxNumber}" Width="*"/>
                        <DataGridTextColumn Header="اللون" Binding="{Binding Color}" Width="*"/>
                        <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="*"/>
                        <DataGridTextColumn Header="الوحدة" Binding="{Binding Unit}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
