<UserControl x:Class="App2.Views.CustomerStatementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             Background="#F8FAFC" 
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryBtn">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
            <StackPanel Orientation="Horizontal">
                <Border Background="#D1FAE5" CornerRadius="8" Width="48" Height="48" Margin="0,0,16,0">
                    <TextBlock Text="📊" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="HeaderTitle" Text="كشف حساب العميل" FontSize="22" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock x:Name="HeaderSubtitle" Text="عرض جميع الحركات المالية للعميل" FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Filters -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0" 
                          x:Name="CustomerCombo"
                          ItemsSource="{Binding Customers}" 
                          SelectedItem="{Binding SelectedCustomer}"
                          DisplayMemberPath="Name"
                          Height="40" Padding="10,0"
                          FontSize="14"
                          BorderBrush="#CBD5E1"
                          Background="#F8FAFC">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBox">
                                        <Grid>
                                            <ToggleButton Name="ToggleButton" 
                                                        Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="1"
                                                        Padding="{TemplateBinding Padding}"
                                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                        ClickMode="Press"/>
                                            <ContentPresenter Name="ContentSite" 
                                                            IsHitTestVisible="False" 
                                                            Content="{TemplateBinding SelectionBoxItem}"
                                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                            Margin="{TemplateBinding Padding}"
                                                            VerticalAlignment="Center"
                                                            HorizontalAlignment="Left"/>
                                            <TextBox Name="PART_EditableTextBox"
                                                    Style="{x:Null}"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Center"
                                                    Margin="{TemplateBinding Padding}"
                                                    IsReadOnly="{TemplateBinding IsReadOnly}"
                                                    Focusable="False"
                                                    Visibility="Hidden"/>
                                            <Popup Name="Popup"
                                                  Placement="Bottom"
                                                  IsOpen="{TemplateBinding IsDropDownOpen}"
                                                  AllowsTransparency="True"
                                                  Focusable="False"
                                                  PopupAnimation="Slide">
                                                <Grid Name="DropDown"
                                                     SnapsToDevicePixels="True"
                                                     MinWidth="{TemplateBinding ActualWidth}"
                                                     MaxHeight="200">
                                                    <Border Background="White" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="4">
                                                        <ScrollViewer Margin="4">
                                                            <ItemsPresenter/>
                                                        </ScrollViewer>
                                                    </Border>
                                                </Grid>
                                            </Popup>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.Style>
                </ComboBox>

                <TextBlock x:Name="AccountNameText" 
                           Grid.Column="0"
                           Text=""
                           VerticalAlignment="Center"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="#0F172A"
                           Visibility="Collapsed"/>

                <DatePicker Grid.Column="1" 
                           SelectedDate="{Binding FromDate}"
                           Height="40" Width="140"
                           Margin="12,0,0,0"
                           FontSize="14"
                           BorderBrush="#CBD5E1"
                           Background="#F8FAFC"/>

                <DatePicker Grid.Column="2" 
                           SelectedDate="{Binding ToDate}"
                           Height="40" Width="140"
                           Margin="12,0,0,0"
                           FontSize="14"
                           BorderBrush="#CBD5E1"
                           Background="#F8FAFC"/>

                <Button Grid.Column="3" 
                       Content="🔄 تحديث" 
                       Style="{StaticResource PrimaryBtn}"
                       Margin="12,0,0,0"
                       Click="Refresh_Click"/>
            </Grid>
        </Border>

        <!-- Summary Cards -->
        <Border Grid.Row="2" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="20" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="الرصيد الافتتاحي" FontSize="14" Foreground="#64748B" FontWeight="Bold"/>
                        <TextBlock Text="{Binding OpeningBalance, StringFormat=N2}" FontSize="24" FontWeight="Black" Foreground="#0F172A" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="20" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="إجمالي المدين" FontSize="14" Foreground="#64748B" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalDebit, StringFormat=N2}" FontSize="24" FontWeight="Black" Foreground="#10B981" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="20" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="إجمالي الدائن" FontSize="14" Foreground="#64748B" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalCredit, StringFormat=N2}" FontSize="24" FontWeight="Black" Foreground="#EF4444" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="3" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="20">
                    <StackPanel>
                        <TextBlock Text="الرصيد النهائي" FontSize="14" Foreground="#64748B" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ClosingBalance, StringFormat=N2}" FontSize="24" FontWeight="Black" Foreground="#3B82F6" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Transactions Grid -->
        <Border Grid.Row="3" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="6">
            <DataGrid ItemsSource="{Binding Transactions}" 
                     AutoGenerateColumns="False"
                     CanUserAddRows="False"
                     CanUserDeleteRows="False"
                     GridLinesVisibility="Horizontal"
                     HeadersVisibility="Column"
                     RowHeight="45"
                     BorderThickness="0"
                     Background="Transparent"
                     AlternationCount="2">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="AlternationIndex" Value="1">
                                <Setter Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTextColumn Header="البيان" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="مدين" Binding="{Binding Debit, StringFormat=N2}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#10B981"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="دائن" Binding="{Binding Credit, StringFormat=N2}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#EF4444"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="الرصيد" Binding="{Binding Balance, StringFormat=N2}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#3B82F6"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
