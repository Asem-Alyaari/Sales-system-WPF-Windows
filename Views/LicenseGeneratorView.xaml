<Window x:Class="App2.Views.LicenseGeneratorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="مولد مفاتيح الترخيص - للمطور فقط" Height="720" Width="620"
        WindowStartupLocation="CenterScreen" Background="#F8FAFC"
        FontFamily="Segoe UI" FlowDirection="RightToLeft"> 

    <Window.Resources>
        <Style x:Key="BaseControlStyle" TargetType="Control">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Border Padding="40" Background="White" Margin="20" CornerRadius="12" Effect="{DynamicResource DropShadowEffect}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,30" HorizontalAlignment="Center">
                <Border Background="#DBEAFE" CornerRadius="40" Width="80" Height="80" Margin="0,0,0,15">
                    <TextBlock Text="🔑" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#2563EB"/>
                </Border>
                <TextBlock Text="مولد مفاتيح الترخيص" FontSize="24" FontWeight="Bold" Foreground="#0F172A"/>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <TextBlock Text="معرف الجهاز (Hardware ID)" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,8"/>
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="HardwareIdTextBox" Height="50" VerticalContentAlignment="Center" FontFamily="Consolas" Padding="10"/>
                    <Button x:Name="CopyHwidBtn" Content="📋" Grid.Column="1" Margin="8,0,0,0" Background="#64748B" Foreground="White" Click="CopyHwidBtn_Click"/>
                </Grid>

                <TextBlock Text="تاريخ الانتهاء" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,8"/>
                <DatePicker x:Name="ExpiryDatePicker" Margin="0,0,0,25"/>

                <TextBlock Text="مفتاح الترخيص الناتج" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,8"/>
                <TextBox x:Name="GeneratedKeyTextBox" Height="150" TextWrapping="Wrap" IsReadOnly="True" 
                         FontFamily="Consolas" Background="#F1F5F9" Padding="15"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                <Button Content="توليد المفتاح" Click="GenerateButton_Click" Background="#2563EB" Foreground="White" Width="180" Height="50" Margin="0,0,15,0"/>
                <Button Content="نسخ المفتاح" Click="CopyButton_Click" Background="#059669" Foreground="White" Width="180" Height="50"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>