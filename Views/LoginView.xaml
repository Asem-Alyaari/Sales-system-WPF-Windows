<Window x:Class="App2.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="تسجيل الدخول"
        Height="550" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F1F5F9"
        FontFamily="Segoe UI"
        FlowDirection="RightToLeft">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CBD5E1"/>
                    <Setter Property="Foreground" Value="#94A3B8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SafeInputStyle" TargetType="{x:Type Control}">
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Control}">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="#10B981"/>
                    <Setter Property="Background" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="White" 
                CornerRadius="12" 
                Padding="25,30">

            <Border.Effect>
                <DropShadowEffect Color="#0F172A" Opacity="0.06" BlurRadius="15" ShadowDepth="2"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,30" HorizontalAlignment="Center">
                    <Border Background="#ECFDF5" CornerRadius="12" Width="65" Height="65" Margin="0,0,0,15">
                        <TextBlock Text="🔒" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#10B981"/>
                    </Border>
                    <TextBlock Text="مرحباً بك مجدداً" FontSize="22" FontWeight="Bold" Foreground="#1E293B" HorizontalAlignment="Center"/>
                    <TextBlock Text="يرجى إدخال بيانات الدخول" FontSize="13" Foreground="#64748B" Margin="0,5,0,0" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Row="1" VerticalAlignment="Center">

                    <TextBlock Text="اسم المستخدم" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,8"/>
                    <TextBox x:Name="UsernameTextBox" 
                             Style="{StaticResource SafeInputStyle}" 
                             Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                             Margin="0,0,0,20"/>

                    <TextBlock Text="كلمة المرور" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,8"/>
                    <PasswordBox x:Name="PasswordBox" 
                                 Style="{StaticResource SafeInputStyle}" 
                                 Margin="0,0,0,30" 
                                 PasswordChanged="PasswordBox_PasswordChanged"/>

                    <Button Content="تسجيل الدخول" 
                            Command="{Binding LoginCommand}" 
                            IsEnabled="{Binding IsLoginEnabled}"/>
                </StackPanel>

                <TextBlock Grid.Row="2" Text="جميع الحقوق محفوظة © 2026" FontSize="12" Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,20,0,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>