<UserControl x:Class="App2.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:App2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#F5F7FA"
             FlowDirection="RightToLeft">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#357ABD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,20" Padding="30,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="📊" FontSize="32" Margin="0,0,15,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="لوحة التحكم" FontSize="28" FontWeight="Bold" Foreground="#2C3E50"/>
                        <TextBlock Text="نظام إدارة المبيعات" FontSize="14" Foreground="#7F8C8D"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="{Binding CurrentDate}" FontSize="14" Foreground="#7F8C8D" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentTime}" FontSize="14" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center" Margin="15,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Statistics Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Card 1 -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0" Padding="25">
                <StackPanel>
                    <TextBlock Text="💰" FontSize="28" Margin="0,0,0,10"/>
                    <TextBlock Text="إجمالي المبيعات" FontSize="12" Foreground="#7F8C8D"/>
                    <TextBlock Text="{Binding TotalSales}" FontSize="24" FontWeight="Bold" Foreground="#27AE60"/>
                </StackPanel>
            </Border>

            <!-- Card 2 -->
            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="0,0,10,0" Padding="25">
                <StackPanel>
                    <TextBlock Text="📦" FontSize="28" Margin="0,0,0,10"/>
                    <TextBlock Text="عدد الطلبات" FontSize="12" Foreground="#7F8C8D"/>
                    <TextBlock Text="{Binding TotalOrders}" FontSize="24" FontWeight="Bold" Foreground="#3498DB"/>
                </StackPanel>
            </Border>

            <!-- Card 3 -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="0,0,10,0" Padding="25">
                <StackPanel>
                    <TextBlock Text="👥" FontSize="28" Margin="0,0,0,10"/>
                    <TextBlock Text="العملاء" FontSize="12" Foreground="#7F8C8D"/>
                    <TextBlock Text="{Binding TotalCustomers}" FontSize="24" FontWeight="Bold" Foreground="#E74C3C"/>
                </StackPanel>
            </Border>

            <!-- Card 4 -->
            <Border Grid.Column="3" Style="{StaticResource CardStyle}" Padding="25">
                <StackPanel>
                    <TextBlock Text="📈" FontSize="28" Margin="0,0,0,10"/>
                    <TextBlock Text="النمو" FontSize="12" Foreground="#7F8C8D"/>
                    <TextBlock Text="{Binding GrowthRate}" FontSize="24" FontWeight="Bold" Foreground="#F39C12"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Recent Activity -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0" Padding="25">
                <StackPanel>
                    <TextBlock Text="📋 النشاط الأخير" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>
                    
                    <ItemsControl ItemsSource="{Binding RecentActivities}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8F9FA" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="20" Margin="0,0,15,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                            <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#7F8C8D"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="2" Text="{Binding Time}" FontSize="12" Foreground="#95A5A6" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Quick Actions -->
            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="25">
                <StackPanel>
                    <TextBlock Text="⚡ إجراءات سريعة" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

                    <Button Content="🧾 فاتورة مبيعات" Command="{Binding ManageSalesInvoicesCommand}" Margin="0,0,0,10" Background="#27AE60"/>
                    <Button Content="� فاتورة مشتريات" Command="{Binding AddPurchaseInvoiceCommand  }" Margin="0,0,0,10" Background="#9B59B6"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
